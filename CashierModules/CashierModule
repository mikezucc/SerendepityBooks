#include <iostream>
#include <string> 
#include "CashierModuleFunctions.h"


using namespace std;

int main()  // void CashierModule()
{
	int choice = 0;
	int TitleISBN;
	string title, ISBN;

	
	cout << "Welcome to the Cashier Module." << endl;
	do
	{
		cout << "Do you wish to buy some books?" << endl;
		cout << "1. Yes \t2. No: ";
		cin >> choice;
		cout << endl;

		if (choice > 3 || choice < 0)
		{
			cout << "invalid Choise, enter a number between 1 - 2!" << endl;
		}

	} while (choice > 3 || choice < 0);

	 if (choice == 2)
	{
		return 0;
	}

	

	TitleISBN = TitleorISBN();

	if (TitleISBN == 1)
	{
		title = InputBookInformationTitle();
	}
	else if (TitleISBN == 2)
	{
		ISBN = InputBookInformationISBN();
	}
	else
	{
		cout << "Error: A unforseen error has occured, Your program will go back to before you entered the Cashier." << endl;
		return 0;
	}



	
	
	/*CategorySetup();

	int Qty;
	string ISBN;
	string Title;
	double Price;
	double Total;

	cin >> Qty;
	cin >> ISBN;
	cin >> Title;
	cin >> Price;
	cin >> Total;

	ListBook(Qty, ISBN, Title, Price, Total); // Display book on screen.
	*/

	
	system("pause");

	
	return 0;
}
